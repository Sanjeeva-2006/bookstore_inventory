# bookstore_inventory
A small Python project to manage a bookstore’s inventory using JSON files. It reads the current inventory, adds new books, saves the updates, and shows the list of books with their details.  This project helps practice working with files, JSON, and basic Python programming.
